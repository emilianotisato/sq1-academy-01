/*function ShowHide(){
    var feedButton = document.getElementById("feedButton")[0];
    var playButton = document.getElementById("playButton")[0];
    var sleepButton = document.getElementById("sleepButton")[0];

    if (!pet["isAlive"]) {
        document.getElementById("petImage").classList.add("deadPet");
        window.alert("GAME OVER");
        playButton.style.visibility == "hidden";
        feedButton.style.visibility == "hidden";
        sleepButton.style.visibility == "hidden";
      }
}*/